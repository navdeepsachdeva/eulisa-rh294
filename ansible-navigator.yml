---
ansible-navigator:
  ansible:
    config:
      path: ./ansible.cfg
      help: false
    # inventory:
    #   entries:
    #   - /home/student/lisa/tasos_inventory
  execution-environment:
    container-engine: podman
    enabled: true
    image: utility.lab.example.com/ee-supported-rhel8:latest
    pull:
      policy: missing
  mode: stdout
  playbook-artifact:
    enable: false
    # save-as: '{playbook_dir}/{playbook_name}-artifact-{time_stamp}.json'
